/**
 * Shared Module - Cross-process types and interfaces
 *
 * This module provides shared types and interfaces used by both
 * main process and renderer process.
 *
 * Usage:
 *   import { AISourceType, AISourceProvider } from '@shared'
 */

export * from './types'
export * from './interfaces'
